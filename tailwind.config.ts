import type { Config } from 'tailwindcss'

export default <Config>{
    theme: {
        extend: {
            fontFamily: {
                sans: ['"Be Vietnam Pro"', 'sans-serif'],
            },
        },
    },
}
